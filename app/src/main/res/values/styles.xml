<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/translucent</item>
        <item name="colorPrimaryDark">@color/olive_green</item>
        <item name="colorAccent">@color/olive_green</item>
        <item name="customChipThin">@style/chip_thin</item>
    </style>

    <style name="toolbar_style">
        <item name="contentInsetStart">0dp</item>
    </style>

    <style name="ProgressThemeDark" parent="Widget.AppCompat.ProgressBar">
        <item name="colorAccent">@color/white</item>
    </style>

    <style name="Style_Dialog">
        <item name="android:windowEnterAnimation">@null</item>
        <item name="android:windowExitAnimation">@null</item>
        <item name="android:windowBackground">@color/translucent_40</item>
        <item name="android:animation">@null</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowSoftInputMode">adjustPan</item>
    </style>


    <style name="Switch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/olive_green</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">#f1f1f1
        </item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">@color/cccccc</item>
    </style>

    <style name="chip_thin_shape" parent="">
        <item name="cornerSize">12dp</item>

    </style>
    <style name="chip_thin_text" parent="TextAppearance.MaterialComponents.Body2">
        <item name="android:textSize">11sp</item>
        <item name="android:letterSpacing">0.07</item>

    </style>

    <style name="chip_thin" parent="@style/Widget.MaterialComponents.Chip.Action">
        <item name="chipMinHeight">18dp</item>
        <item name="chipMinTouchTargetSize">24dp</item>
        <item name="chipIconSize">13dp</item>
        <item name="closeIconSize">13dp</item>
        <item name="textStartPadding">4dp</item>
        <item name="textEndPadding">4dp</item>
        <item name="android:textAppearance">@style/chip_thin_text</item>
        <item name="shapeAppearanceOverlay">@style/chip_thin_shape</item>

    </style>

  <style name="BottomNavigationView">
        <item name="itemTextAppearanceActive">@style/TextAppearance.BottomNavigationView.Active</item>
        <item name="itemTextAppearanceInactive">@style/TextAppearance.BottomNavigationView.Inactive</item>
        <item name="fontFamily">@font/noto_sans</item>
    </style>


    <!-- blank styles for better code readability-->
    <style name="TextAppearance"/>
    <style name="TextAppearance.BottomNavigationView"/>

    <!-- inactive tab icon style -->
    <style name="TextAppearance.BottomNavigationView.Inactive">
        <item name="android:textSize">12sp</item>
        <item name="android:letterSpacing">0.1</item>
    </style>

    <!-- active tab icon style -->
    <style name="TextAppearance.BottomNavigationView.Active">
        <item name="android:textSize">12sp</item>
        <item name="android:letterSpacing">0.1</item>
    </style>

</resources>
